import 'package:flutter/material.dart';

class DemoStateLessClass extends StatelessWidget {
  const DemoStateLessClass({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
